# W1-04 EBリセット接続確認（2026-09-25）

## 実装

SpaceのPress Only Actionを追加し、`OnEbReset` → 注目編成の`Input.ResetEb()` → 有効運転台の`EbDevice.RequestReset()`へ接続した。要求は次の入力収集でスナップショットに取り込み、1 tickだけ消費する。作動前の無操作時間は0秒に戻す。作動後の非常保持と「停車・力行なし」での自動解除は維持する。

対象EBは編成内の車両割り当てとTIMSの有効運転台で選ぶ。欠損・無効・重複を拒否し、他編成の装置を除外する。装置を無効化した場合は未消費の要求を破棄する。

## 検証結果

Unity 6000.4.0f1の独立した検証用プロジェクトへ現行ソース・Input Actionsをコピーし、全ソースをコンパイルした。対象EditModeテストは74件成功、失敗0件。

| Fixture | 成功数 |
| --- | ---: |
| EbDeviceLogicTests | 38 |
| EbDeviceResetTests | 4 |
| EbResetInputTests | 10 |
| TimsBrakeNewLogicTests | 22 |

Input Systemのテスト用KeyboardからSpaceの押下・保持・解放を送り、押下時のみの要求、注目先なし、有効運転台変更後の送り先を確認した。Controllerのキュー消費、入力収集後に届いた要求の次tickへの持ち越し、EB作動後の解除条件も検証した。EditModeのAwake・OnDisableはテストから明示的に呼ぶ。

実プロジェクトのNtLineではPlayを停止し、未保存変更がないことを確認した上でPlayerInputへ6番目のイベントを追加・保存した。`Driving/EbReset` → `OnEbReset`の接続を再取得して確認済み。シーン差分は当該イベント1件のみ。ゲーム内での走行操作による手動検証は未実施。

変更ソース・Input Actionsが検証用コピーと一致すること、追加アセットのmeta、Action GUIDとシーン参照を確認した。対象コード・文書のdiffチェックは成功。リポジトリ全体のdiffチェックには、作業前からのシーンYAMLの末尾空白が残る。
