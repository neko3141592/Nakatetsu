# 2026年11月17日に向けた週別実装計画

- 作成日：2026-09-21
- 対象：Nakatetsu／2026-09-21〜2026-11-17（8週間＋2日）
- 作業条件：本人1人・週20〜25時間、Codex Pro 5xを利用
- 調査基準：ブランチ `neko3141592/実装計画`、HEAD `d70c018`

## 1. 到達方針

駅一覧の確認により、今回の制作範囲を中鉄中央（NT01）〜猿沼（NT11）、11駅・営業キロ10.6kmに確定した。[駅一覧・種別別速度・残件](Track/NtLineSpecification.md)を参照。

**10月18日までに10.6km・11駅を走れる運行基盤、11月1日までにsnapshotから始まる3シナリオ、11月8日までに最低限の景観を含む完成候補を作る。11月9日以降は不具合修正と配布確認に使う。**

現状は車両機器とTIMSの実装が先行しており、「車両が線路上を走るゲーム」への統合がこれからになる。車両内部の再現度をさらに上げるより、走行→保安→ダイヤ→snapshot→ゲーム進行をつなぐことを優先する。

この計画は**Codexでコード作成・テスト・データ制作補助を進め、人間が仕様判断・Unity統合・試運転を担う、やや攻めた見積もり**である。週20〜25時間なら無条件で間に合う、という保証ではない。第2週・第4週の到達判定で、景観の作り込みと運行の複雑さを早めに削る。

### 11/17版の完成条件

| 項目 | 必須の完成状態 | 初期版で抑える範囲 |
| --- | --- | --- |
| 路線 | 中鉄中央（NT01）〜猿沼（NT11）の営業距離10.6km、停車可能な11駅。全区間を連続運転できる | 複線を基本案とし、分岐は終端付近など必要箇所に限定。正式な配線は第1週で固定 |
| 簡易CTC | ダイヤ・列車位置・進路条件に基づく進路要求、待機理由表示、占有・進路状態の簡易表示 | 高機能な指令卓、運休整理、任意のダイヤ編集UIは作らない |
| 連動 | 競合進路の排除、占有中・鎖錠中の転換禁止、開通条件確認、列車後端通過後の解錠 | 小規模な進路表で実装。進入後の任意取消は受け付けず、必要時はシナリオを再開 |
| デジタルATC | 地上から停止限界・制限情報を受け、車上で連続的な速度パターンを計算。超過時に実ブレーキへ介入 | 特定実機の通信電文・全故障モード・装置操作は再現しない |
| 車両・操作 | 既存車種1種類。前方視点、力行・ブレーキ・戸扱いなど運転に必要な入力と表示 | TIMS画面操作・機器開放・起動儀式は不要。開始時に自動準備 |
| 運行 | 昼の約1時間分のダイヤ。自列車とNPC列車が同じ閉塞・進路ルールで動く | 各停中心、固定の番線・経路。高度なATO、複雑な折返しや入換は後回し |
| シナリオ | ダイヤ運行を一定間隔でsnapshot化し、そのうち3件から運転を開始 | 初期版は駅停車中のプレイヤー列車を選ぶ。任意セーブ機能は不要 |
| ゲーム進行 | 選択→読込→運転→完了→結果→選択へ戻る。再挑戦・中断が可能 | 採点は停止誤差・遅延・保安装置介入回数など最低限 |
| 景観 | 全駅にホーム・駅名・停止目標。沿線に線路小物と簡素な街並みがあり、運転席から大きな空白や破綻が目立たない | 共通Prefab・箱形建物・繰返し素材中心。リアルさを追求しない |
| 配布 | 対象PC向けビルドをUnity Editorなしで起動し、3シナリオを完走できる | 対象OS・基準PCは第1週に1組決める |

CTCの時刻到達だけで信号を開通させない。**ダイヤは進路を要求し、連動が許可し、ATCが走行限界を守る**構成にする。画面の信号表示だけ切り替わる実装は完成扱いにしない。

## 2. 現在の実装状況

### 調査範囲と限界

`Assets/Nakatetsu` のC#、定義Asset、Prefab一覧、`NtLine.unity` の保存内容、既存設計文書、Packages・Build Settingsを確認した。対象内にC#ファイル152件、`*Tests.cs` 19件がある。

**この計画作成時にはUnityの起動、コンパイル、テスト実行、実機での描画確認は行っていない。** 以下の「実装あり」はソース・保存アセットの存在と内容の確認であり、現在のHEADで動作確認済みという意味ではない。最初の週に基準ビルドと既存テストの結果を記録する。旧 `TD-ATC` の動作や移植可能量も今回新たには検証していない。

### 機能別の棚卸し

| 領域 | 確認できた実装・資産 | 完成目標との差分 |
| --- | --- | --- |
| 開発環境 | Unity `6000.4.0f1`、URP、Input System、Test Framework。Build Settingsは `NtLine.unity` 1件 | 起動フロー、対象OSでのビルド・実行確認が必要 |
| 車両機器 | マスコン、運転台選択、VVVF、モーター、空制、荷重、ドア、速度センサー、EB | 新規に作り直さず使用。ゲーム用入力、ATC接続、snapshot復元を追加 |
| 車両物理 | `TrainPhysicsLogic` が力・質量から加速度と符号付き速度を計算。`TrainSimulationController` が機器・物理を進める | 距離積分→線路上移動→各車姿勢への接続が必要。勾配等の入力も走行側から接続 |
| TIMS | Local/Master Bus、力行・制動・ドア連携、速度計等の表示。ATCブレーキを扱う入力フィールドがある | ATC装置そのものや地上系は確認できない。ATC入力の収集・欠損時処理・表示との実接続が必要 |
| 車体・運転台 | Series1000のFBX・車体Prefab・モニター資産がある | `TrainPresentationBuilder.Awake()` はグラフ待ちのTODO。走行編成・カメラ・運転UIへの組込みが必要 |
| 基準線形 | `TrackGeometry` の直線・円曲線・緩和曲線・勾配評価、Compiler、Preview、テスト | 実線路のオフセット評価・距離表・移動処理は別途必要 |
| グラフ | Geometry・Edge・Node定義の集約、参照検証、ID検索Context、Edge Asset、一定オフセットとLUT保存用データがある | LUT生成・実長評価・走行評価・分岐選択・通過履歴・編成占有は未接続 |
| 路線・駅 | Track配下で確認できた線形Assetは `TrackGeometryStraight100m.asset`。NT01/NT02の駅フォルダに実アセットなし | 10.6km・11駅の配線・駅データと表示を制作する必要がある |
| 地上小物・街 | 出発時機表示機のPrefab・表示スクリプト、標識等のBlender制作元がある。Worldにはasmdefのみ | 全線のレール・ホーム・街並み配置、制作元の書出し、Prefab化が必要。制作元の存在を完成小物数に数えない |
| 運行・保安 | 地上CTC・連動・閉塞・車上ATCの実装は対象C#内で確認できない | 一連の仕組みを新規実装または範囲を絞って移植 |
| ダイヤ・シナリオ | Applicationにはasmdefのみ。ダイヤ・シナリオ・運行snapshotの実装は確認できない | 時計、NPC運行、データ定義、保存復元、ゲーム進行が必要 |
| 既存snapshot | `TimsBusState.GetSnapshot/ReplaceSnapshot` とテストがある | Bus値の複製機能。路線上の列車、時計、進路鎖錠、機器内部状態の復元機能ではない |

### 計画に影響する具体的な注意点

1. **100m線形では現行編成を収められない。** `Consist.asset` は20m車×10両で約200m。単車試験の次に、500m以上の線路と全編成を載せられる初期位置を用意する。
2. **時間進行を早期に外から呼べるようにする。** 現在は `TrainSimulationController.Update()` → private `Step(Time.deltaTime)`。ダイヤ時計、複数列車、早送り、snapshotを後付けすると更新順が崩れやすい。第1週で手動tick経路を設ける。
3. **文書とコードの差分がある。** 実装ルールは `ISimulationController.CollectInput()` 等を説明するが、現在のInterfaceは `Calculate` と `ApplyOutput` の2メソッド。Physicsも `SetInput()` で入力を受ける。全機器の一括改修を始めず、現行接続を確認して世界全体の更新契約を先に決める。
4. **既存の線路方針を維持する。** [線路基盤の移植計画](./Migration/TrackFoundationMigration.md) はオフセット方式採用を明記している。締切前に別方式の線路エディターを作り直さない。
5. **ATOの設計資料は実装完了を意味しない。** NPC用の簡単な自動運転は必要だが、プレイヤー用ATO/TASCの本格実装は今回の必須範囲から外す。

### 主な調査根拠

文書の設計案より、今回のHEADのコードを現在地の根拠として優先した。

- [TrainSimulationController](../Assets/Nakatetsu/Train/Simulation/Orchestration/Scripts/TrainSimulationController.cs)、[Physics](../Assets/Nakatetsu/Train/Simulation/Physics/TrainPhysicsLogic.cs)、[Simulation Interface](../Assets/Nakatetsu/Train/Simulation/Orchestration/Interfaces/ISimulationController.cs)
- [TrackGeometryCalculator](../Assets/Nakatetsu/Track/Graph/Geometry/Scripts/TrackGeometryCalculator.cs)、[TrackGraphDefinition](../Assets/Nakatetsu/Track/Graph/Shared/Scripts/TrackGraphDefinition.cs)、[TrackGraphCompiler](../Assets/Nakatetsu/Track/Graph/Shared/Scripts/TrackGraphCompiler.cs)
- [100m線形](../Assets/Nakatetsu/Track/Graph/Geometry/Data/TrackGeometryStraight100m.asset)、[編成定義](../Assets/Nakatetsu/Train/Consist/Definitions/Data/Consist.asset)、[車体生成のTODO](../Assets/Nakatetsu/Train/Presentation/Shared/TrainPresentationBuilder.cs)
- [TIMS制御](../Assets/Nakatetsu/Train/Equipment/Tims/Composition/Scripts/TimsControlController.cs)、[ノッチ調停](../Assets/Nakatetsu/Train/Equipment/Tims/Notch/Scripts/TimsNotchLogic.cs)、[Bus snapshot](../Assets/Nakatetsu/Train/Equipment/Tims/Bus/Scripts/TimsBusState.cs)
- [NtLineシーン](../Assets/Scenes/NtLine.unity)、[Build Settings](../ProjectSettings/EditorBuildSettings.asset)、[Unityバージョン](../ProjectSettings/ProjectVersion.txt)
- [実装ルール](./Architecture/ImplementationRules.md)、[運転デバッグの範囲](./Train/DebugControls.md)、[ATO/TASC方針](./Train/AutomaticOperation/AtoTascImplementation.md)

## 3. 工数と進め方

### 使う時間

以下は**本人の実働時間**。設計、Codexへの依頼、出力のレビュー、Unity設定、試運転を含む。Codexが自動で処理している待ち時間は含めないが、結果の確認時間は含める。倍率による一律短縮はしていない。

| 期間 | 本人の予定時間 | うち予備時間 | 到達点 |
| --- | ---: | ---: | --- |
| 第1週 9/21〜9/27 | 24h | 3h | 全編成が短区間を走る |
| 第2週 9/28〜10/4 | 24h | 3h | 閉塞・連動と2駅の運転ループ |
| 第3週 10/5〜10/11 | 24h | 3h | ATC・簡易CTC・NPCの接続 |
| 第4週 10/12〜10/18 | 24h | 3h | 10.6km・11駅、昼1時間の運行 |
| 第5週 10/19〜10/25 | 24h | 3h | 一定間隔snapshot生成・復元 |
| 第6週 10/26〜11/1 | 24h | 3h | 3シナリオを選択して完走 |
| 第7週 11/2〜11/8 | 20h | 2h | 景観・操作性を含む完成候補 |
| 第8週 11/9〜11/15 | 20h | 12h | 回帰確認・修正・配布候補固定 |
| 最終 11/16〜11/17 | 4h | 4h | 最終確認・納品 |
| **合計** | **188h** | **36h** | 予定作業152h＋予備36h |

第1〜6週は上限寄りの24時間を必要とする。毎週20時間になる場合は、8週＋最後の4時間で164時間となり、24時間不足する。その場合は第9節の簡略化を第1週から適用する。予備36時間を最初から新機能に割り当てない。

週24時間の配分例は、平日3時間×4日＋週末6時間×2日。週前半に仕様とロジック、週中に統合、週末に路線・景観作業と通し運転を置く。9/21の残り時間が少ないため、第1週の着手は9/22を中心に調整する。

### 依存関係

```text
線形・グラフ → 編成移動・通過履歴 → 列車全長の占有 → 連動 → 地上ATC → 車上ATC
                  ↑                         ↑
既存の車両機器・Physics           時計・ダイヤ → 簡易CTCの進路要求
                  └──────── NPC運転・停車処理 ────────┘
                                      ↓
                         運行世界のsnapshot生成・復元
                                      ↓
                          シナリオ選択・手動運転・完了

短区間のゲーム進行は第2週から仮データで作り、上記の実データへ順次差し替える。
路線・景観は走行基盤の確定後、毎週少しずつ制作する。
```

## 4. 週ごとの詳細計画

時間欄は各週の合計に含まれる。タスクIDはIssueやCodexへの依頼単位として使える。大きい項目は実装と接続確認で2件程度に分ける。

### 第1週：9/21（月）〜9/27（日）— 全編成を線路上で走らせる

前提：現在のHEAD。まず既存機器が使えることを確かめる。

**W1-01は2026-09-22完了。** [検証結果・確定条件・後続への引継ぎ](Validation/W1-01-baseline-2026-09-21.md)を参照。W1-03以降とW1全体の合格判定は未完了。

**W1-02は2026-09-22完了。** 世界時計、固定tick、自動更新、停止・再開、再生倍率、手動tick、デバッグUIを実装。[世界時計と固定tickの設計](Architecture/WorldTick.md)と[確認結果・snapshot対象State・引継ぎ](Validation/W1-02-world-tick-2026-09-22.md)を参照。次はW1-03の線路上移動へ進む。

**W1-03の位置・方向・移動結果の基本契約は2026-09-22確定。** [線路上移動の契約](Architecture/TrackMovement.md)を参照。基準点は編成定義の先頭車中心、移動距離の正方向は固定された編成前方向とする。終端制限は基準点だけで判定し、車体のはみ出しによる移動制限は今回含めない。W1-03の実装・検証完了を意味しない。

同日の中間実装は[Track定義・一定オフセットの検証記録](Validation/W1-03-track-foundation-2026-09-22.md)を参照。定義整理と保存・検索の基盤までを対象とし、走行処理は後続作業。

| ID | 順序・時間 | 作業と成果物 |
| --- | --- | --- |
| W1-01 | 前半・3h | Unityコンパイル、既存EditModeテスト、現行Sceneで加減速を確認。対象OS・基準PC・配線案・駅一覧・最高速度を固定。旧TD-ATCの使える路線データを読取調査し、使えなければ簡略線形の新規データ制作に決める |
| W1-02 | 前半・4h | `Application` 側の世界時計と固定tickの入口を作る。既存Trainの手動Step・自動更新停止を接続し、停止・再開・高速実行の入口を用意。snapshot対象Stateの一覧も作る |
| W1-03 | 中盤・6h | Geometry参照の解決、実距離評価、Edge内移動・直列Edge跨ぎ・終端結果、編成通過履歴を実装。まずオフセット0で通し、複線用の定数オフセットと距離表を接続 |
| W1-04 | 後半・5h | Physics→Dynamics→Track→車体Prefabの姿勢更新を接続。500m以上の試験線に10両を置き、簡易レール、前方カメラ、キーボード運転入力、速度・ノッチ表示を作る |
| W1-05 | 週末・3h | 境界・後退・終端・大きな移動量のテストとPlayMode確認。独立ビルドを1度作る。以後使う試運転記録を残す |
| W1-B | 随時・3h | コンパイル・Prefab参照・既存機器の不具合対応 |

**週末の合格条件**：10両編成が500m以上の区間で発進・制動・停車できる。Edge境界で車体が飛ばず、後退で車両順序が変わらない。Inspectorのデバッグボタンを使わずに操作できる。停止中は世界時計・車両とも進まない。

Codexへ任せる中心：移動ロジック、境界テスト、tick入口の局所改修。本人の中心：既存機器の起動確認、車体・カメラ接続、配線の決定。

### 第2週：9/28（月）〜10/4（日）— 閉塞・連動と最小のゲーム進行

前提：W1の移動と全編成位置。短い分岐試験線で検証する。

| ID | 順序・時間 | 作業と成果物 |
| --- | --- | --- |
| W2-01 | 前半・5h | 分岐の定位・反位・未検知、分岐オフセット線形、編成が実際に通った経路の保持を接続。旧方式の必要な形状だけを移植し、汎用線路エディターは作らない |
| W2-02 | 前半・4h | 閉塞定義と占有計算。先頭・後端の間にある全Edge区間を列挙し、停止中・分岐跨ぎ・後退でも占有が欠けないようにする |
| W2-03 | 中盤・5h | 最小進路表と連動状態機械。要求→条件確認→転換完了確認→鎖錠→開通→通過確認→解錠。競合進路、占有、未検知、取消制約をテスト |
| W2-04 | 後半・4h | 仮ホーム2駅・停止位置・停車判定・戸扱いを作る。仮シナリオ1件で選択→運転→完了→戻る。ゲーム開始時に運転台を自動準備し、操作案内を表示 |
| W2-05 | 週末・3h | 手動進路要求の簡易画面で占有・鎖錠・開通を確認。2駅間をビルドで往路運転。信号未開通時の先行立入禁止処理も確認 |
| W2-B | 随時・3h | 移動・分岐・UIの統合修正 |

**週末の合格条件**：2駅間の運転をゲームの画面操作だけで開始・完了できる。競合する2進路は同時開通しない。列車が分岐上にいる間は転換・早期解錠されない。ここでは本ATCは翌週だが、テスト中も停止限界を無視して進める状態を残さない。

Codexへ任せる中心：閉塞・連動の状態遷移とテスト、最小UIコード。本人の中心：進路表・分岐配置、ホームと運転操作の確認。

### 第3週：10/5（月）〜10/11（日）— ダイヤ要求からATC介入までつなぐ

前提：W2の占有・連動。保安の接続を最優先し、装置の細部は増やさない。

| ID | 順序・時間 | 作業と成果物 |
| --- | --- | --- |
| W3-01 | 前半・4h | 地上ATC用データを作る。確保済み経路、先行列車の占有、未開通進路から停止限界を求め、速度制限・有効性・更新tickとともに車上へ渡す |
| W3-02 | 前半〜中盤・6h | 車上ATCの速度パターン、速度超過検出、力行抑止、常用・非常介入と解除条件を実装。ブレーキ応答遅れ・勾配を含む余裕を設定し、TIMS Bus経由で既存ブレーキ・速度計へ接続 |
| W3-03 | 中盤・4h | ダイヤ定義（列車ID・駅/番線・着発・経路）と簡易CTCを実装。時刻＋接近条件で要求、競合時は待機、理由を表示。仮の15〜20分ダイヤで2列車を動かす |
| W3-04 | 後半・4h | NPCの速度追従・停止目標・停車時間・発車条件を実装。共通の運転指令入口を使い、手動との切替を準備。世界Stateの試験用保存・読戻しで時計・位置・鎖錠の抜けを早期確認 |
| W3-05 | 週末・3h | 先行列車停車・進路待ち・情報失効を含む試運転。ATC介入で停止限界手前に止まり、先行列車が進めば運転を再開できることを確認 |
| W3-B | 随時・3h | ATCの減速調整・NPCの停車不具合対応 |

**週末の合格条件**：ダイヤの要求が連動を通って信号・走行限界に反映される。少なくとも2列車が同じ運行世界に存在し、追突せず、進路待ちから復帰する。ATC表示だけでなく、実際のブレーキ圧・減速度に介入が現れる。簡易保存で全状態を扱える見通しが立つ。

Codexへ任せる中心：ATC・ダイヤ・CTCのロジックと異常系テスト。本人の中心：制動距離の試運転、簡易運行の確認。W3-01/02を先に完成させ、遅れた場合は景観時間を使ってNPC側を回収する。

### 第4週：10/12（月）〜10/18（日）— 10.6km・11駅と昼1時間のダイヤ

前提：W3の保安・NPC運行。ここで路線の構造を固める。

| ID | 順序・時間 | 作業と成果物 |
| --- | --- | --- |
| W4-01 | 前半・6h | 営業区間10.6kmと終端余裕を含む線形、11駅の停止位置・番線・閉塞・進路・速度制限を入力。重複ID、切れたEdge、短すぎる有効長、存在しない進路を検出するValidatorを追加 |
| W4-02 | 前半〜中盤・4h | レール・道床・ホームの共通Prefabを全線配置。駅名、停止位置目標、主要標識を付ける。地形や街は仮形状でよい |
| W4-03 | 中盤・4h | 実測した駅間走行時間＋停車時間＋余裕から昼1時間のダイヤを作る。例は12:00〜13:00・各停10〜15分間隔。列車本数と同時在線数を確定し、始端での生成・終端での退出を定義 |
| W4-04 | 後半・4h | 全線手動運転と、1時間分のNPC運行を実行。閉塞競合・停車時分・線形の破綻を修正。初期在線列車、ウォームアップ、シナリオ終了までの時刻余裕を決める |
| W4-05 | 週末・3h | 基準PCで全線を測定。車体・軌道の揺れ、速度計、メモリ・フレーム時間を記録。snapshotの路線ID・定義ハッシュを固定する |
| W4-B | 随時・3h | 路線データ・ダイヤの調整 |

**週末の合格条件**：10.6km・11駅を手動で完走できる。約1時間のダイヤをNPCで進めても、原因不明の永久進路待ち、占有重複、路線外移動が起きない。運転に必要な全駅の停止目標が見える。分岐・閉塞IDの変更を原則凍結する。

Codexへ任せる中心：データValidator、繰返し配置補助、運行ログ集計。本人の中心：線形・ホーム調整、ダイヤを組むこと、全線試運転。

### 第5週：10/19（月）〜10/25（日）— snapshot生成と再開を完成させる

前提：W4の全線運行、W1/W3で整理した保存対象。Busの保存だけでは完成にしない。

| ID | 順序・時間 | 作業と成果物 |
| --- | --- | --- |
| W5-01 | 前半・5h | version付きsnapshot DTOと全状態のCapture/Restoreを実装。列車の通過履歴・機器内部State・進路鎖錠・待機要求・ダイヤ進捗まで含める |
| W5-02 | 前半〜中盤・4h | 固定tickでダイヤを実行し、一定間隔で保存する生成ツールを作る。標準は60秒間隔、設定で変更可能。生成時刻・定義ハッシュ・検証結果をmanifestへ記録 |
| W5-03 | 中盤・4h | 復元順序・データ不一致検出・中途失敗時の破棄を実装。プレイヤー対象列車のNPC→手動切替と、自動運転指令が残らないことを確認 |
| W5-04 | 後半・5h | 同じsnapshotから2回再開、連続運行対復元後運行、分岐跨ぎ・進路待ち・停車中のケースを比較。保存直後だけでなく数分先まで不一致を追う |
| W5-05 | 週末・3h | 3シナリオ候補のsnapshot・列車ID・終了駅を選定。全線の沿線地面・共通建物の配置試作も行う |
| W5-B | 随時・3h | 保存漏れ・初期化順序・参照再構築の修正 |

**週末の合格条件**：ダイヤを走らせて一定間隔のsnapshot群を生成できる。任意の検証対象を読んで占有・鎖錠・時刻・列車位置を整合させて再開できる。プレイヤー介入後はNPC・CTC・ATCが現在の状況に追従する。

Codexへ任せる中心：シリアライズ、状態比較、復元・検証コード。本人の中心：開始状況の選定、再開直後の運転感覚・表示・ドアの確認。

### 第6週：10/26（月）〜11/1（日）— 3シナリオを通して遊べる状態にする

前提：W5のsnapshot生成・復元。11/1を機能追加の締切とする。

| ID | 順序・時間 | 作業と成果物 |
| --- | --- | --- |
| W6-01 | 前半・4h | シナリオ定義と選択画面を実データ化。snapshot ID・担当列車・開始時刻・終了駅・目安時間を表示し、ロード失敗から選択へ戻れるようにする |
| W6-02 | 前半〜中盤・4h | 運転HUD、次駅・予定時刻・停止位置、停止・戸開・戸閉・発車の判定、運転完了条件を統合。終着で結果へ遷移 |
| W6-03 | 中盤・4h | 結果表示・再挑戦・中断・一時停止・操作案内を整備。異常操作やオーバーラン時の再試行導線を作り、編集画面に戻らず遊べるようにする |
| W6-04 | 後半・5h | 第7節の3シナリオを制作し、各1回以上手動完走。スナップショット選択、ダイヤ余裕、評価閾値を調整。退行や大幅遅延時の扱いを固定 |
| W6-05 | 週末・4h | 全駅の共通装飾と、全区間の最低限の街並み・線路小物を配置。必要な運転音・警報・戸閉音を既存素材から接続 |
| W6-B | 随時・3h | シナリオの進行不能・残留状態の修正 |

**週末の合格条件**：ビルドで3シナリオすべてを「選択→運転→完了→選択」で一巡できる。再挑戦しても前の列車・占有・時計・音が残らない。営業区間10.6km全区間に最低限の景観がある。以後、必須目標を満たすための不足補完と不具合修正以外の機能を増やさない。

Codexへ任せる中心：ゲーム状態遷移、評価・データ整合、繰返し配置補助。本人の中心：全シナリオの運転、UIの見やすさ・景観・音量調整。

### 第7週：11/2（月）〜11/8（日）— 景観と遊びやすさの仕上げ

前提：全シナリオ完走済み。新しい運行方式を入れない。

| ID | 順序・時間 | 作業と成果物 |
| --- | --- | --- |
| W7-01 | 前半・6h | 街並み、架線柱/柵などの小物、駅周辺、遠景を共通素材で整える。全線を区間ごとに確認し、地面の穴・浮き・レールやホームとの食込みを直す |
| W7-02 | 中盤・3h | ATC表示、停止目標、操作案内、効果音を調整。初見相当の手順で運転し、操作に迷う箇所を修正 |
| W7-03 | 中盤〜後半・4h | 最大同時在線数で性能測定。重い描画・NPCモニター・不要な更新を絞る。長距離精度に問題があれば表示側だけの原点移動を限定導入 |
| W7-04 | 後半・3h | 3シナリオと開始直後・終着・進路待ちの回帰確認。操作説明、既知の制約、素材ライセンス一覧を整える |
| W7-05 | 週末・2h | 完成候補RC1を作り、別フォルダへの展開・起動から確認。致命的問題と軽微な問題を分けて記録 |
| W7-B | 随時・2h | 性能・表示・音の修正 |

**週末の合格条件**：完成目標をすべて含むRC1がある。通常操作で進行不能がなく、基準PCの暫定目標（WQHD（2560×1440）で概ね60fps、継続的な30fps未満なし）を確認する。解像度・品質設定を含む実測条件を記録し、平均fpsだけで合否を判断しない。

### 第8週：11/9（月）〜11/15（日）— 回帰確認と配布候補固定

| ID | 順序・時間 | 作業と成果物 |
| --- | --- | --- |
| W8-01 | 前半・3h | snapshot再生成、定義ハッシュ照合、必要な単体・統合テストを実行。無変更のデータまで不用意に更新しない |
| W8-02 | 中盤・3h | 3シナリオを通常運転で各2回確認。追加で意図的遅延・ATC介入・中断再開・再挑戦を試す。運転時間が想定を超える場合は予備から検証時間を追加 |
| W8-03 | 後半・2h | 配布ビルド、操作説明、既知の問題、バージョン、起動確認結果をまとめ、11/15に配布候補を固定 |
| W8-B | 随時・12h | 進行不能・snapshot不整合・保安の不具合・ビルド問題の修正と再確認。余った時間だけ軽微な見栄え修正 |

**週末の合格条件**：第8節の最終チェックを満たす配布候補があり、再現する致命的不具合が残っていない。修正した経路と隣接する経路を再テストし、無関係な全項目の反復で時間を使い切らない。

### 最終：11/16（月）〜11/17（火）— 最終確認

4時間を最後の予備とする。配布候補を新規フォルダから起動し、3シナリオの読込・操作・完了が可能な成果物を確定する。新しいモデル・線路・ライブラリを入れない。最後の修正が不要なら、そのまま11/15候補を完成版とする。

## 5. 実装を膨らませないための設計方針

### 所有者と配置

以下は新規実装の配置案。既存asmdefの依存方向に従い、必要な機能を追加する時点でフォルダを作る。

| 所有者 | 責務 |
| --- | --- |
| `Track` | Geometry/Graph、分岐、経路上距離、駅・閉塞、占有、連動、地上ATC |
| `Train/Simulation` | 既存機器とPhysics、速度から移動距離への積分。Trackとの接続は適切な上位Adapterを介する |
| `Train/Equipment/Safety` | 車上ATCの入力有効性、速度パターン、制動要求 |
| `Train/Equipment/Tims` | ATC状態・制動要求をBusで収集し、既存指令調停・表示へ接続 |
| `Application` | 世界時計、全列車の更新順、ダイヤ、CTCの進路要求、NPC指令、snapshot、シナリオ・UI |
| `World` / `Track/Presentation` | 背景、視点、線路・駅・小物の表示。運行Stateを書き換えない |

各列車が別々の `Update()` で世界時計を進めない。固定tickの例は0.02秒だが、第1週の既存機器確認で決定する。早送りはdeltaTimeを巨大化させず、同じtickを複数回実行する。

更新順は、前tick確定位置から占有→CTC要求→連動→地上ATC→車上ATC/手動/NPC/TIMS→機器/Physics→移動→占有再計算・イベント確定→snapshot可能境界、として明文化する。先行列車との距離にはtick遅れも含む余裕を取る。同一tick中に列車ごとに共有占有を随時書き換えて順番依存を作らない。

### 保安と運転の最小仕様

- ATCは「路線制限、分岐制限、停止限界までの減速パターン」の最も厳しい値を使う。0速度目標は経路距離で扱い、別線の見かけ上近い列車を誤検知しない。
- 制動パターンは実測した常用制動性能を下回る前提で設定する。応答遅れ・下り勾配・位置余裕を考慮し、最高速度で停止限界手前に止まることを試運転で確かめる。
- 無効・失効したATC情報、未検知分岐、未確保経路では力行を許可せず、規定の停止要求を出す。これらはゲーム内モデルの仕様であり、実設備規格への適合を主張しない。
- NPCは通常の運転指令として力行・常用制動を要求する。ATC介入用フィールドをNPCの通常ブレーキに流用しない。
- 初期版のNPCは同じ車両シミュレーションを使う方針で開始する。性能が不足した場合だけ簡易モデルを検討し、占有・連動・ATC・保存契約は共通にする。
- 駅停車成功は、例として速度0.1m/s未満かつ停止目標±3m以内で判定し、戸扱い・停車時間を経て発車可能にする。閾値はゲーム用の暫定設定として第6週に確定する。
- 正常な最終駅停車と必要な戸扱いで運転完了。軽微な遅延は結果に記録し、回復不能なオーバーラン・線路端逸脱などは理由を示して再挑戦できるようにする。
- 終端で複雑な折返しを組む代わりに、プレイ区間外の生成・退出区間を使える構成にする。列車全長が抜ける前に占有を消さない。プレイヤーの全線運転には終着までの有効長を確保する。

## 6. ダイヤとsnapshotの仕様

### 生成の流れ

```text
路線定義＋編成定義＋昼ダイヤ＋初期在線列車
  → ウォームアップを含む固定tick運行（全列車NPC）
  → tick確定境界で60秒ごとにsnapshotを保存
  → 検証済みsnapshot一覧
  → snapshot ID＋担当列車ID＋終了条件からシナリオを定義
  → 起動時に世界を復元し、対象列車だけ手動へ切替
```

1時間の本編を `[12:00, 13:00)` とする場合、60秒間隔なら12:00〜12:59の60件が基本となる。13:00の終端確認用を含めるなら別に1件と明記する。間隔は設定値とし、開始状況が選びづらい場合のみ30秒へ変更する。

12:00から空の路線に列車を順次出すだけでは昼ダイヤの途中状態にならない。運転開始前から在線している列車を初期定義するか、例えば11:40から運行を進めて12:00以降を保存する。必要なウォームアップ時間は全線走行時間で決める。

最後のシナリオの完了時刻＋遅延余裕まで運行データを用意する。例として12:30開始・25分運転なら12:55完了を狙う。遅延で13:00を越える場合も進路を閉じたりNPCを突然消したりせず、終端まで継続する。必要なら前後の補助ダイヤを足すが、制作の中心は昼1時間分とする。

### 保存対象

| 分類 | 保存する値 | 復元時の扱い |
| --- | --- | --- |
| 共通 | schemaVersion、route/timetable/vehicle定義のIDとハッシュ、固定tick幅、tick番号・時刻、乱数State（乱数を使う場合） | 定義不一致はロードエラーにし、無理に読まない |
| 列車 | 永続Train ID、運行ID、編成、位置基準点、Edge IDと実距離、編成向き、通過履歴、速度、次駅・停車段階・遅延 | 通過履歴から全車位置・編成全長占有を再構築 |
| 車両内部 | マスコン、ドア状態とタイマー、BC圧・モーター等の応答State、EB・非常保持、ATC内部State、必要な通信の前tick値 | 次tickの振る舞いに影響する状態を監査し、担当Logicに明示的な復元入口を設ける |
| 地上 | 分岐位置・転換途中状態、進路鎖錠・列車との対応・通過段階、未処理進路要求 | 占有だけから鎖錠状態を推測しない。保存した状態と再計算結果を照合 |
| 運行 | 各列車のダイヤ進捗、停車・発車・生成・退出のイベント消化状況、NPC制御State | 消化済みイベントを二重実行しない |

Transform、MonoBehaviour参照、描画物、辞書キャッシュをそのまま保存しない。復元は「時計停止→定義検証→列車・機器生成→State適用→経路・占有再構築→鎖錠照合→Bus/表示更新→担当列車の操作権変更→tick開始」の順に行う。

初期版の3件は担当列車が駅停車中のsnapshotを選び、手動切替時のノッチを中立・停止保持へ安全に整える。これは明示した開始時の変更としてログに残し、他列車や地上状態を初期化し直さない。一般の生成snapshotには走行中・分岐跨ぎ列車も含まれるため、保存対象から動的状態を除外しない。

### 復元の合格条件

- 同一バージョン・同一実行環境・同一入力で、連続運行と保存復元後運行の進路・占有・イベント順が一致する。
- 位置・速度等は暫定許容値（例：位置1cm、速度0.01m/s）で比較する。誤差を許容しても、閉塞・信号の離散状態が変わるケースは不合格とする。
- 分岐跨ぎ、先行列車待ち、停車・戸開中、BC加圧中を含める。少なくとも復元後5分間を比較する。
- 別OS間のビット単位の完全一致は初期版の目標にしないが、配布先OSで3件すべての運転成立を確認する。
- snapshotは**初期状態**として使う。開始後の世界を録画のように再生しない。プレイヤーが遅延したらNPC・CTC・連動・ATCを再計算する。

## 7. 初期3シナリオ案

駅名・発時刻は第4〜6週のデータ確定時に置き換える。以下は、1時間のダイヤから成立する範囲で選ぶ制作案。

| シナリオ | snapshotの選び方 | 運転区間・目安 | 体験と検証目的 |
| --- | --- | --- | --- |
| 1. 昼間の全線各停 | 例：12:00、始発駅停車中 | 10.6km・11駅、20〜30分 | 全線が運転可能であること、基本操作、定時運転 |
| 2. 昼間の区間運転 | 例：12:15、中間駅で停車中の別列車 | 4〜6駅、10〜15分 | 途中時刻・途中位置から自然に始められること、短く遊べること |
| 3. 先行列車に続く運転 | 例：12:30、先行列車との間隔が短い駅停車時 | 4〜6駅、10〜20分 | ATCによる速度抑制、進路待ち、開通後の運転再開 |

第3シナリオ用の条件は元ダイヤの停車時分・発車間隔の中で作り、別スクリプトで信号を赤に固定して演出しない。全線走行が30分を超えそうなら、駅間時分と開始時刻を調整し、完了までの運行データを確保する。

## 8. 検証と完成判定

新しいテストは、停止限界・競合排除・保存復元など壊れるとゲームが成立しない部分に集中する。表示文言や単純な装飾の変更に一律でテストを増やさない。

| 層 | 主な確認 | 実施時期 |
| --- | --- | --- |
| Logic/EditMode | 距離積分の一重実行、Edge境界・後退、全長占有、競合進路、解錠、ATC失効・超過、ダイヤイベント順 | 第1〜3週で追加、以後関連変更時 |
| 統合/PlayMode | 世界tickの一重実行、TIMS→実ブレーキ、NPCの停車・発車、snapshot Capture/Restore、手動への切替 | 第2〜5週で段階追加 |
| 運行データ | 全駅・閉塞・進路の参照解決、番線有効長、1時間運行、開始前後の列車・ダイヤ | 第4週以降 |
| 手動運転 | 操作・視認性、停車・戸扱い、ATC制動、シナリオ完了・再挑戦 | 毎週末、最終は配布ビルド |
| 性能・配布 | 基準PCの全線と最大在線、長距離座標、連続再挑戦、別フォルダからの起動 | 第4・7・8週 |

### 最終チェックリスト

- [ ] 営業距離10.6km・11駅を、線路切断や車体ワープなく運転できる。
- [ ] 全駅にホーム・停止目標・駅名があり、運転席から必要な小物と街並みを確認できる。
- [ ] ダイヤ→CTC要求→連動の許可→信号/走行限界→ATC制動が実際につながっている。
- [ ] 競合進路、占有中転換、列車後端通過前の解錠が発生しない。
- [ ] 先行列車・未開通進路に接近するとATCが実制動を行い、停止限界を越えない。
- [ ] 昼約1時間のダイヤから一定間隔で生成したsnapshotと、その生成設定・manifestが残っている。
- [ ] 配布する3件すべてで復元検証が成功し、参照している定義ハッシュが一致する。
- [ ] 3シナリオを選択→運転→完了→結果→選択まで進められる。
- [ ] 再挑戦・中断・一時停止で列車・イベント・音・UI状態が重複しない。
- [ ] TIMS操作・機器開放なしでゲームを開始・完了できる。
- [ ] 配布先OS・基準PCで起動し、操作説明だけで遊べる。継続的な処理落ちと致命的エラーがない。
- [ ] 既知の軽微な問題と対象外機能を記載し、ビルド・コード・路線・snapshotのバージョンを対応付けている。

## 9. 遅延した場合の判断

### 節目ごとの判断

| 判定日 | 遅延の兆候 | その時点で行うこと |
| --- | --- | --- |
| 9/27 | 既存機器が動かない／編成が線路上を走らない | 第2週の装飾を削り、既存機器の復旧と短区間走行を優先。線形編集機能・カント・精密な車体姿勢の追加を止める |
| 10/4 | 分岐・全長占有・競合排除が成立しない | 分岐数・進路表を最小化し、追越し・入換・複雑な折返しを外す。連動そのものは省略しない |
| 10/11 | ATC実制動・2列車運行がつながらない | 路線の独自形状や装飾より保安の接続を優先。NPCの運転方式を単純化し、最大同時在線数を絞る |
| 10/18 | 全線・1時間ダイヤが動かない | 共通ホーム・共通建物に統一。終端は生成/退出方式に限定し、特別な駅構造を削る。第7週の仕上げ時間を基盤修正へ移す |
| 10/25 | snapshot復元に占有・鎖錠の不一致がある | 新しいシナリオ演出を止め、保存対象漏れ・tick境界・初期化順を修正。担当列車は停車中開始に限定したまま維持 |
| 11/1 | 3シナリオが完走しない | 第7週の見栄え改善を打ち切り、進行不能を修正。全線1本＋短区間2本の構成を維持 |
| 11/8 | 必須条件に未達がある | 残りの予備を必須条件へ集中。なお満たせない場合は未達を明記し、目標達成と扱わない |

### 週20時間ペースになった場合の24時間削減案

以下は通常案から削る制作・調整作業。該当する週のタスクを縮小して使い、修正用の予備は残す。

| 簡略化 | 削減目安 | 保つもの |
| --- | ---: | --- |
| 第1〜2週：旧データの広範な移植や多種類の分岐形状をやめ、必要な数種類だけに固定 | 4h | 10両走行、必要な分岐と連動 |
| 第3〜4週：各停・固定経路・終端生成退出に限定し、ダイヤ密度を抑える | 4h | 約1時間の運行と先行列車の存在 |
| 第4〜6週：11駅のホーム・標識・建物を完全に共通化し、手作業での差別化を省く | 8h | 10.6km・11駅、全区間の最低限の景観 |
| 第6〜7週：結果画面の装飾、追加の環境音、駅ごとの演出、遠景の作り込みを省く | 8h | 3シナリオ、結果表示、必要な警報・操作案内 |
| 合計 | 24h | 完成目標の機能は維持 |

削らないものは、全長・駅数の目標、CTC/連動/ATCの実接続、snapshotの整合性、3シナリオ、通し検証。優先して延期するものは、TIMS操作、機器開放、故障再現、プレイヤーATO/TASC、詳細な車両電気モデルの追加、精密カント、汎用ダイヤエディター、旅客AI、天候・夜間、駅ごとの高精細モデルである。

## 10. Codex Pro 5xの使い方

公式説明ではProはPlusに対して5倍または20倍のCodex利用枠を選べる。利用消費はモデル、タスクの規模・複雑さ、文脈などで変わり、週単位の制限もあり得る。**5xは開発速度5倍・5人分の作業能力を意味しない。** この計画では利用回数の固定保証や追加クレジット購入を前提にしない。出典：[OpenAI公式 Pricing](https://learn.chatgpt.com/docs/pricing)（2026-09-21確認）。

### 任せる作業と本人が確認する作業

| Codexへ依頼する作業 | 本人が確認すること |
| --- | --- |
| Logic/Context、状態機械、DTO、Validator、保存復元、境界ケースのテスト | 停止限界・鎖錠条件・単位・方向・データ所有者が仕様通りか |
| 路線・ダイヤの繰返しデータ、配置スクリプト、ログ解析 | 線形の見た目、ホーム有効長、運行時分、通しで運転した感触 |
| 既存機器へのAdapterと小さいUIの実装 | Unityの参照、更新順、Prefab、実ビルドでの動作 |
| 変更後の差分レビュー、再現手順からの不具合調査 | 再現が消えたか、他のシナリオへ影響していないか |

同じScene/Prefabを複数の作業で同時に編集しない。別の作業を走らせる場合も、共有契約を先に決め、独立したC#やテストへ範囲を限定し、統合とレビューは1件ずつ行う。

### 1回の依頼を小さくする

1件は「入力契約＋期待結果＋検証方法」を明記できる大きさにする。本人のレビュー・接続確認が30〜90分程度で終わる粒度を目安とし、毎週4〜6個の作業枠をさらに必要な範囲で分割する。大量のコードを一度に生成して週末にまとめて接続しない。

依頼例：

```text
対象：W2-02 編成全長の閉塞占有
前提：Documentation/implementation-plan-2026-11-17.md と
      Documentation/Architecture/ImplementationRules.md を確認する。
入力：先頭基準点、編成寸法、実際に通過したEdge列、閉塞範囲。
出力：占有閉塞ID集合と不正経路の診断。
条件：後退しても編成向きを反転しない。分岐跨ぎでも中間閉塞を落とさない。
変更範囲：占有Logic/Contextと関連テスト。Scene/Prefabは変更しない。
完了：3閉塞跨ぎ、境界一致、後退、履歴不足のテスト結果と変更理由を示す。
```

### 利用枠の配分

- 第1〜5週は依存関係の多い走行・保安・snapshotの実装とレビューを優先し、文書の繰返し書直しや無関係なリファクタリングに使い過ぎない。
- 単純なデータ整形・小規模コードには軽い設定、曖昧な契約・不具合調査・保安レビューには推論を厚くする設定を選ぶ。特定のモデル名に日程を依存させない。
- 毎週、利用状況と「提案のうち実際に採用できた変更」を確認する。待ち時間より本人のレビュー滞留が増えたら、同時に依頼する量を減らす。
- 利用枠に達した日は路線配置・試運転・ダイヤ入力・素材調整へ切り替える。未レビューの変更をまとめて取り込んで日程を埋めない。
- 完了報告には変更ファイル、実行したテスト、未実行の確認、残る不具合を必ず残す。Codexの「実装完了」と週末の「遊べるビルド」を分けて管理する。

## 11. 最初に着手する順序

2026-09-21：W1-01に着手。[基準動作確認・制作条件・残件](Validation/W1-01-baseline-2026-09-21.md)を記録。対象はmacOS（Apple Silicon）、メモリ16GB以上・M2以上推奨、営業最高速度120km/h。駅名・キロ程と中鉄中央〜猿沼の11駅・10.6kmの範囲は確定。[NT線仕様](Track/NtLineSpecification.md)を参照。配線基本案は本人作成SVGを採用。2026-09-22にW1-01完了。旧線形は利用せず新規制作し、設計上の性能基準機はMacBook Air M2・16GB、推奨解像度はWQHD（2560×1440）とする。

1. 現行HEADでUnityを起動し、コンパイル・既存19テストファイルの実行結果・運転デバッグの基準状態を記録する。
2. 10.6km・11駅の配線案、対象OS、基準PCを決め、制作とデータ入力の範囲を固定する。
3. `TrainSimulationController` の手動tick入口を作り、世界時計から1回ずつ進められるようにする。
4. 500m以上の試験線でGeometry→Edge移動→10両配置を通す。
5. 既存の力行・制動を移動と前方カメラへ接続し、第1週の走行ビルドを完成させる。

以後は各週の合格条件を満たしたビルド、テスト結果、残件、消費時間を週末に記録し、次週のタスクと予備時間を更新する。
